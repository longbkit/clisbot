Work only in the configured repository and use its scoped GitHub authentication. Follow the reply
instructions for every user-facing message. Make the requested change and run the smallest relevant
checks.

Immediately after sending the answer or final status, call `hub.finish_execution`. Do not wait for
another command.
