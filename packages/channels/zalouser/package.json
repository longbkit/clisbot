{
  "name": "@getpaseo/channels-zalouser",
  "version": "0.1.0",
  "private": true,
  "description": "In-repo Zalo Personal (`zalouser`) channel vertical (goal ledger slice 17). Ported from extensions/zalouser/src@5d8067a4483: the zca-js client boundary, the QR login state machine, the credential/session closure, the markdown text-style compiler, the send path, the message-tool `react` action, the `zalouser` agent tool, the friend/group directory and the probe. Upstream's SDK is zca-js, pinned at upstream's version.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsgo -p tsconfig.json",
    "typecheck": "tsgo --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@getpaseo/channels-core": "*",
    "@getpaseo/channels-shared": "*",
    "typebox": "1.3.18",
    "zca-js": "2.1.2",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@typescript/native-preview": "7.0.0-dev.20260423.1",
    "vitest": "^3.2.4"
  }
}
