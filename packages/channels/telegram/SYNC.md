# Telegram vertical — sync manifest

Module → OpenClaw **TypeScript source** reference (`~/projects/openclaw-private/extensions/telegram/src/`), one pass each. The pinned compiled dist under `~/.clisbot-dev/channels/work/` is NOT the reference; it remains a supply artifact (blueprint §6.5 hard rule 3: after the pull it is a sync reference for zalouser only, the two pulled channels point at this in-repo dist).

| In-repo module                       | OpenClaw source reference                                                                                                                                                                                                                                                                                                                                 | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/client/bot-api.ts`              | `send-message.ts` (text path only), `outbound-adapter.ts` (`baseOpts` + `TELEGRAM_TEXT_CHUNK_LIMIT = 4000`), `send-context.ts` surface                                                                                                                                                                                                                    | Pre-existing L1 (written against the pinned `send-BgA996pw.js` chunk); this pass kept its API and applied the coordinator pre-flight fixes: async grammy factory (no `require`), dead void-pins removed, `sendTelegramText` trimmed under 50 lines by extracting `buildChunkParams` (per-chunk thread params from the shared `buildTelegramThreadParams` + chunk-0-only reply params from `buildTelegramReplyParams` — F-07/D-016; `parse_mode: HTML` per chunk on the rich path). The rich send path reuses the C5 front-end (`markdownToTelegramHtml` + `splitTelegramHtmlChunks`). G7–G11 addition: the `TelegramApi` interface gained the six media methods (`sendPhoto`/`sendDocument`/`sendAudio`/`sendVoice`/`sendVideo`/`sendAnimation`), and `createTelegramApi` now consults a token-keyed fake-API override (`registerTelegramApiForTest`, mirror of the Slack vertical's `registerSlackWriteClientForTest`) so the outbound media path is unit-testable without a real grammy client (D-014). Poll/sticker/inline-keyboard/location/reaction surface intentionally NOT ported (D-004). |
| `src/client/sent-messages.ts`        | `sent-message-cache.ts` (`recordSentMessage` store half)                                                                                                                                                                                                                                                                                                  | Re-targeted at the keyed-store seam (D-001).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `src/leaves/telegram-policy.ts`      | `send-error-predicates.ts`, `network-errors.ts` (429/5xx classification), `retry-after.ts` boundary, `fetch.ts` (`TelegramTransport`), `accounts.ts` (apiRoot)                                                                                                                                                                                            | Error classification is duck-typed (no grammy import at type level); the throttler (`@grammyjs/transformer-throttler`, pinned dep) loads lazily at `createTelegramAccountThrottler` (D-005). Retry-after cap decided at 60 s (D-002).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `src/leaves/coerce.ts`               | `targets.ts` (`parseTelegramTarget` / chat-id normalization), `error-runtime` (`formatErrorMessage`), chat-not-found wrap from `prepareTelegramOutbound`'s error path                                                                                                                                                                                     | The `:topic:<id>` suffix grammar is decided (D-006).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `src/leaves/rich-message.ts`         | `format.ts` (plain-text chunking); `send-message-text.ts` (per-chunk thread/silent + single-use reply-to)                                                                                                                                                                                                                                                 | Plain-text chunker ported at 4000 + the shared send-param builders both send paths (and the media post) use: `buildTelegramThreadParams` (per-chunk `message_thread_id` + silent) and `buildTelegramReplyParams` (chunk-0-only `reply_parameters` — F-07/D-016: a topic send must carry `message_thread_id` on the plain path too, on EVERY chunk).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `src/format.ts`                      | `format.ts` (`markdownToTelegramIR` → `renderTelegramHtml` → `renderSupportedTelegramHtml`; markdown-it token walk)                                                                                                                                                                                                                                       | The C5 markdown → Bot API HTML front-end; `sendTelegramText` reuses it by default (markdown → Bot API HTML; an account's explicit `richMessages: false` opts out to plain text — D-003). Transcript-role-header + file-reference wrapping passes omitted (no such surfaces in the drive path). Unit-verified byte-for-byte against OpenClaw's `markdownToTelegramHtml` in `format.test.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `src/format-sanitize.ts`             | `format.ts` (`renderSupportedTelegramHtml` + tag-support tables + `telegramHtmlToPlainTextFallback` + `countTelegramHtmlVisibleCharacters`)                                                                                                                                                                                                               | Keeps only Bot API `parse_mode: HTML`-legal tags; escapes the rest so a stray tag in agent output cannot break the send. The raw-HTML escape pass (`escapeUnsupportedTelegramHtml`) is not ported — no `textMode: "html"` surface in the drive path (D-004).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `src/format-html.ts`                 | `format-html.ts` (entity decode + entity-end finder) + `escapeTelegramHtml`                                                                                                                                                                                                                                                                               | HTML entity helpers + the `&`/`<`/`>` escape both `format.ts` and `format-sanitize.ts` import (the cycle-breaker — `format.ts` already imports from `format-sanitize.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `src/telegram-html-chunk.ts`         | `format.ts` (`splitTelegramHtmlChunks` + `splitTelegramHtmlChunksRaw` + split-index helpers)                                                                                                                                                                                                                                                              | Tag-balance-aware HTML chunking: a long `<b>`/`<pre><code>` scope is closed + reopened around the 4000-char boundary so no chunk carries a dangling entity or unbalanced tag. The rich send path uses the legacy 4000 limit, not OpenClaw's 32 768 rich-blocks limit (`send-message-text.ts`: `maxChars = useRichMessages ? … : 4000`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `src/html-tags.ts`                   | `packages/markdown-core/src/html-tags.ts` (`tokenizeHtmlTags`)                                                                                                                                                                                                                                                                                            | markdown-it's quote-aware `HTML_TAG_RE` grammar, inlined so the vertical stays self-contained; sticky-regex loop instead of the source's slice-per-tag.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `src/transport/poll.ts`              | `monitor-polling.runtime.ts` (module surface), `polling-session.ts` (poll loop shape: read offset → getUpdates long-poll → persist max offset → back off on recoverable fault), `update-offset-store.ts` (offset state + rotation drop), `allowed-updates.ts` (`resolveTelegramAllowedUpdates`), `token-fingerprint.ts` (fingerprint + bot-id-from-token) | Direct-fetch loop, grammy-free (the pinned host monitor is fetch-only — `docs/audits/pinned-vertical-contracts/telegram-monitor.md`). The pinned `polling-session.ts` also carries the ingress spool/worker/liveness machinery, which belongs to the excluded native ingress worker and is not ported (D-004). Offset store: one key per account store root, version field dropped (D-007). E2 addition (2026-08-28): `callback_query` updates are parsed (via `approval-callback.ts`), acked first with a silent `answerCallbackQuery`, and handed to the `onApprovalCallback` option; a transport-level `update_id` seen-set is the redelivery guard (the hub's exactly-once latch is the second line). `allowed_updates` is now sent on the getUpdates URL with `callback_query` added (the pinned monitor omitted the param — full-set default; see D-013).                                                                                                                                                                                                                                    |
| `src/transport/approval-callback.ts` | — (in-repo seam, COMPAT(clisbot-control-plane); mirrors `extensions/slack/src/transport/approval-card.ts` of the in-repo Slack vertical, which in turn mirrors the pinned Slack `block_actions` envelope)                                                                                                                                                 | The Telegram half of the approval card's button-click seam (E2): narrows the `callback_query` wire update to who clicked (`from.id`), the opaque `callback_data` (parsed ONCE in the hub — the vertical never parses the card value), the root chat id, the optional topic `message_thread_id`, and the chat type; `approvalCallbackRootKind` normalizes `private` → `dm`, everything else → `group` (the plane's root-descriptor table for Telegram).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `src/lifecycle/start-account.ts`     | `bot-info.ts` (getMe shape), `bot-info-cache.ts` (fingerprint-keyed cache, 24 h TTL), `startup-probe-limiter.ts` (2-slot probe limiter)                                                                                                                                                                                                                   | Pre-monitor flow per `docs/audits/pinned-vertical-contracts/start-account.md`: token → duplicate-token guard → getMe probe (cached) → L2 handoff. Cache value is `{tokenFingerprint, fetchedAt:number, bot}` — the seam store is JSON-native, so `fetchedAt` is epoch ms, not the source's ISO string (D-008). E2 addition (2026-08-28): `telegramApprovalCallback` wires `ctx.channelRuntime.approvalAction` to the poll's `onApprovalCallback` option (the mirror of the Slack vertical's `slackApprovalInteractive`) — absent seam = no card clicks (unit posture).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `src/outbound.ts`                    | `outbound-adapter.ts` (`resolveTelegramSendContext`'s `baseOpts` shape: cfg/accountId/threadId→messageThreadId/replyTo→replyToMessageId)                                                                                                                                                                                                                  | `plugin.outbound.sendText` entry; chat-id resolution + `recordSentMessage` write-back into the seam (D-001). Threads the account's `richMessages` flag (on by default, D-003) + `messageThreadId` / `replyTo` into `sendTelegramText`; reply-text media paths are not auto-attached (D-017). The explicit Hub `send_file` MCP tool routes to `sendMedia` (G7–G11), which runs the shared G11 gate FIRST and hands accepted files to `sendTelegramMedia` + `recordSentMessage` write-back (D-001).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `src/outbound-media.ts`              | `outbound-media.ts` (`resolveTelegramOutboundMediaSenders` mime→method routing), `packages/media-core/src/constants.ts` (`mediaKindFromMime` mime-kind mapping) + `mime.ts` (`EXT_BY_MIME` ext→mime)                                                                                                                                                      | The one-file media post (`sendTelegramMedia`): `readFileSync` → grammy `InputFile`, then the mime-routed Bot API method via `withTelegramSendRetry` (the shared G11 cap + notice live in `shared/media-policy.ts`). The source's photo→document fallback ladder + `forceDocument`/`asVoice`/`sendImageAsPhoto` toggles are not ported (D-014). `telegramMediaMethod` is the pure mime→method table (gif→sendAnimation, image→sendPhoto, video→sendVideo, audio/ogg or opus→sendVoice, audio→sendAudio, else→sendDocument), unit-tested against the OpenClaw routing. `message_thread_id` rides via the shared `buildTelegramThreadParams`.                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `src/runtime-store.ts`               | — (in-repo seam)                                                                                                                                                                                                                                                                                                                                          | Holds the `HostRuntime` (filled by `entry.setChannelRuntime`) and the per-account shared-L3 processor; the L4 hands poll events to it, outbound reads the runtime.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `src/plugin.ts`                      | pinned entry shape (`telegramPlugin` export name, B3)                                                                                                                                                                                                                                                                                                     | `gateway.startAccount` + `outbound.sendText` + `outbound.sendMedia` (the G7–G11 native-media seam, D-014).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `src/entry.ts`                       | pinned entry shape                                                                                                                                                                                                                                                                                                                                        | `createChannelEntry` (shared), id `"telegram"`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

Deps pinned in `package.json` (unchanged): `grammy@1.44.0`, `@grammyjs/runner@2.0.3`, `@grammyjs/transformer-throttler@1.2.1`. The grammy stack is only loaded at real-client construction (`createTelegramApi`'s default factory) and throttler creation; the L2 poll loop and shared-L3 inbound path are grammy-free.
