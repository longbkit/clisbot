{
  "name": "@getpaseo/channels-googlechat",
  "version": "0.1.0",
  "private": true,
  "description": "In-repo Google Chat channel vertical (goal ledger slice 14). Ported from extensions/googlechat/src@5d8067a4483: the Chat API REST client, the service-account auth + token cache, the markdown→Google Chat renderer, target/space resolution and the webhook request handler. Third-party SDKs are upstream's: google-auth-library.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsgo -p tsconfig.json",
    "typecheck": "tsgo --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@getpaseo/channels-core": "*",
    "@getpaseo/channels-markdown-core": "*",
    "@getpaseo/channels-shared": "*",
    "google-auth-library": "11.0.2",
    "undici": "8.10.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@typescript/native-preview": "7.0.0-dev.20260423.1",
    "vitest": "^3.2.4"
  }
}
