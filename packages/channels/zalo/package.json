{
  "name": "@getpaseo/channels-zalo",
  "version": "0.7.3-beta.1",
  "private": true,
  "description": "In-repo Zalo Official Account (Bot API) channel vertical (goal ledger slice 16). Ported from extensions/zalo/src@5d8067a4483: the Bot API client, token/account resolution, the probe, the send path, the message-tool actions and the webhook request handler with its durable-admission spool parsing. Zalo ships no SDK; upstream hand-rolls the client over fetch and depends only on zod.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsgo -p tsconfig.json",
    "typecheck": "tsgo --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@getpaseo/channels-core": "*",
    "@getpaseo/channels-shared": "*",
    "undici": "8.10.2",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@typescript/native-preview": "7.0.0-dev.20260423.1",
    "vitest": "^3.2.4"
  }
}
