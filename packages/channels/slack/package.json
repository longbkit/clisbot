{
  "name": "@getpaseo/channels-slack",
  "version": "0.1.0",
  "private": true,
  "description": "In-repo Slack channel vertical (blueprint docs/audits/2026-08-26-in-repo-channel-verticals.md §6.5). L1 thin @slack/web-api wrapper (the npm dep is pinned — the tarball's bundled node_modules are NOT vendored) + L2 Socket Mode + L4 lifecycle + outbound. Sync reference: @openclaw/slack@2026.7.1 dist.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsgo -p tsconfig.json",
    "typecheck": "tsgo --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@getpaseo/channels-shared": "*",
    "@slack/socket-mode": "2.0.7",
    "@slack/web-api": "7.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@typescript/native-preview": "7.0.0-dev.20260423.1",
    "vitest": "^3.2.4"
  }
}
