# wave3-e4e6 lane log (E4 + E6: hub-attached channel-reply MCP tool + per-route outbound-path toggle)

## 2026-08-28 02:19 UTC — lane started

Scope: E4 (hub-attached `message` MCP tool via http loopback MCP endpoint) + E6
(per-route outbound-path toggle, default OFF, prompt-injection template).
Zero-daemon-diff: packages/hub only (packages/channels only if the outbound
seam genuinely lives there).

Design decisions from main (fixed, not re-decided):

- mcpServers entry: http → `http://127.0.0.1:<hubPort>/mcp/channel/<opaque-binding-ref>`
- tool `message(action=send, text, final)`; final=false = progress, final=true/omitted = completed
- tool posts through the vertical's outbound seam (same path as relay), ledger row recorded
- route knob: `outbound: { path: "relay" | "tool", template?: string }`, org-floor default `relay`
- OFF ⇒ byte-identical create_agent config (no mcpServers, no toolPolicy, no systemPrompt)
- ON ⇒ mcpServers + toolPolicy.preapproved grant + systemPrompt injection template;
  relay sync folded to {finalAnswers:false, progress:false, toolCalls:false} for tool-path turns
- Exactly-one outcome: tool post is the user-visible answer; relay silent on tool-path turns

## 2026-08-28 02:20 UTC — recon starts

Reading: protocol messages.ts, hub daemon/types.ts, control-plane.ts, config
schema/compile, execution.ts, relay, daemon MCP server code, OpenClaw
system-prompt.ts:596-613 reference.

## 2026-08-28 03:12 UTC — recon complete, code first (step a)

All recon claims verified: wire shape (messages.ts AgentSessionConfigSchema:
toolPolicy/systemPrompt/mcpServers), hub CreateAgentConfig already declares
all three; MCP SDK @modelcontextprotocol/sdk ^1.30 is a hub dependency with
reference impl at execution-capabilities/server.ts (per-request Server +
WebStandardStreamableHTTPServerTransport, stateless, close on response);
loopback trust in channels/http/operations.ts authorized(); port = readPort()
in index.ts (CLI hub start sets PORT=6868); relay sync read from
context.route.defaults.sync; ledger recordDelivery/confirmDelivery in
db/channels.ts; C9 lane in-flight edits to schema.ts/compile.ts/execution.ts/
relay/index.ts visible in tree (their sync.subagents) — my edits compose.

Design locked (no re-decisions, matches main's pinned header):

- Route knob: `outbound: { path: "relay" | "tool", template?: string }` on
  RouteSchema + ChannelDefaultsSchema + DefaultsLayer; ORG_DEFAULTS floor
  `{ path: "relay" }`.
- compile.ts foldDefaults: `outbound` leaf folds like the rest; when the
  effective path is "tool", the tool-path sync fold applies at the compile
  plane: sync {finalAnswers:false, progress:false, toolCalls:false} (relay
  stays silent for tool turns; relay/index.ts itself untouched).
- control-plane.ts createChannelAgentSpecResolver gains the route + outbound
  (resolver signature: (target, outbound) where outbound is the effective
  route outbound config); OFF => identical 5-field config as today (byte
  identical create_agent test); ON => mcpServers {channelReply: {type:"http",
  url: http://127.0.0.1:<hubPort>/mcp/channel/<ref>}} + toolPolicy.preapproved
  [{kind:"mcp", server:"channelReply", tool:"message"}] + composed
  systemPrompt (default ported OpenClaw messageToolOnly text, overridable via
  outbound.template).
- hubPort plumbed: supervisor/composition reads process.env.PORT (same source
  as index.ts readPort(), default 3000; live hub = 6868) and passes it into
  createChannelAgentSpecResolver options.

Next: read fresh schema.ts (C9 edits), edit schema.ts + compile.ts +
control-plane.ts, then targeted tests.

## 2026-08-28 (local) — step b wiring complete

- operations.ts: `handleChannelReplyMcp` op + `gateChannelReplyMcp` (flag off -> 404 problem; server null -> shared 503 `database_unavailable`; unauthorized -> 401 problem; else `server.handle(request, token)` with error mapping). Options gained `channelReplyServer: ChannelReplyServer | null`.
- app.ts: `HubOperations.handleChannelReplyMcp`, ops literal entry, `createChannelControlPlaneOpsFor` threads `options.channelReplyServer ?? null`.
- routes/mcp/channel/$token.ts: NEW — POST-only, token = raw pathname segment 3 (base64url, no encoding needed).
- routeTree.gen.ts: manual add (import, update(), RootRouteChildren + rootRouteChildren, ByFullPath/ByTo/ById, 3 unions, FileRoutesByPath). Regenerated by build:start.
- VERDICT: step b (endpoint plumbing) code complete; tests + EffectiveDefaults literal fixes pending.

## 2026-08-28 (local) — step c finished

Recon on disk confirmed step b complete; found + fixed the remaining step c gaps:

- `channel-reply.ts` / `channel-reply.test.ts`: wrong relative import depth (was
  written from `src/` perspective; actually in `src/channels/`). Also: the test
  helper shadowed itself (`const fixture = fixture()` → TDZ) → renamed helper to
  `makeFixture`; MCP `setRequestHandler` handlers typed `unknown` don't satisfy
  the SDK's `ServerResult` union → `ListToolsRequestSchema` handler returns
  `ListToolsResult`, `CallToolRequestSchema` handler returns `Promise<CallToolResult>`.
- `control-plane.test.ts`: URL slice bug in the round-trip test (sliced from
  offset 0 instead of after the `/mcp/channel/` prefix); added a typed
  `channelReplyServerEntry` helper (wire `mcpServers` values are `unknown`);
  `kind` literal widened in the byte-identity test.
- `bindings/index.ts` (the real step c bug): `createAgent` body referenced
  out-of-scope `account` while the param was `accountId: string` — threw at
  runtime on every bind ("agent create in progress"). Added a `channel:
P0ChannelName` param, threaded from `firstMention`'s `account.channel`.
  New test asserts the resolver receives the full binding ref incl. channel.
- `plane/types.ts`: missing `EffectiveDefaults` import for `AgentSpecResolver`.
- Gate tests (flag off → 404 problem; server null → shared 503; non-loopback
  without Bearer → 401 Bearer; Bearer authorized → passthrough; server throw →
  500 problem) added to `http/control-plane.test.ts`; `stubSupervisor` + the
  disposal-test supervisor stub gained `channelReplyPost` (new required
  interface method).
- hubPort plumbing verified end-to-end: index.ts `readPort()` sets
  `process.env.PORT` → `hubListenPort(process.env)` in control-plane.ts
  → resolver options → `http://127.0.0.1:${hubPort}/mcp/channel/${token}`.
  All EffectiveDefaults literals in tests already carry `outbound` (no stale
  literals left).

Test evidence (all `npx vitest run <file> --bail=1` from packages/hub, single-threaded):

- channel-reply.test.ts 7/7; control-plane.test.ts 12/12; http/control-plane.test.ts 13/13;
  compile.test.ts 18/18; bindings.test.ts 18/18; relay.test.ts 17/17;
  execution.test.ts 8/8; policy.test.ts 45/45.
- `npm run typecheck:node` (hub, under .tsgo.lock): exit 0.
- lint: oxlint exit 0 on all changed files; biome 0 errors, 15 format fixes
  applied via `npm run format:files`.
- Re-ran channel-reply + control-plane + http/control-plane after formatting:
  all green.

VERDICT STEP C: GREEN

## 2026-08-28 (local) — final gate pass

- typecheck:node (hub, under .tsgo.lock): exit 0 GREEN.
- lint (7 changed files): 0 warnings, 0 errors.
- format:files applied; channel-reply.test.ts re-run post-format 7/7.
  VERDICT STEP C: GREEN (confirmed by clean typecheck).
